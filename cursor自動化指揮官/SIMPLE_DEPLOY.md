# 🚀 超簡單部署指南

> **一鍵複製，立即使用！**

---

## ✨ 最簡單的方式

**所有檔案都已經統一放在 `cursor自動化指揮官` 資料夾中！**

### 只需要一個指令：

```bash
# 複製整個資料夾到你的專案
cp -r cursor自動化指揮官 /path/to/your/project/.cursor-commander
```

**完成！** 就是這麼簡單！

---

## 📋 接下來三步驟

### 1️⃣ 設定環境變數

```bash
cd /path/to/your/project
cp .cursor-commander/rag/.env.example .cursor-commander/rag/.env
# 編輯 .cursor-commander/rag/.env 填入實際值
```

### 2️⃣ 安裝依賴（如果需要 RAG 功能）

```bash
cd .cursor-commander/rag
npm install
cd ../..
```

### 3️⃣ 測試

```bash
node .cursor-commander/scripts/automate.mjs analyze
```

---

## ✅ 完成！

現在你可以開始使用了：

```bash
# 列出所有指令
node .cursor-commander/scripts/automate.mjs list

# 執行 RAG 流程（如果有資料）
./.cursor-commander/cmd start

# 開啟 API 快捷中心
open .cursor-commander/web/index.html
```

---

## 📁 複製的資料夾包含什麼？

```
.cursor-commander/
├── core/              # 核心自動化系統
├── scripts/           # 執行腳本
├── rag/               # RAG 資料處理
├── supabase/          # 資料庫遷移
├── web/               # API 快捷中心網頁
├── automation_commands.json
├── package.json
├── cmd                # 一鍵入口
└── 所有說明文件
```

**所有需要的檔案都在裡面，直接複製就好！** 🎉

---

## 💡 提示

- 不需要選擇性複製，直接複製整個資料夾最簡單
- 所有檔案都已經整理好，結構完整
- 複製後只需要設定環境變數即可使用

---

需要更詳細的說明？查看 `DEPLOY.md`
